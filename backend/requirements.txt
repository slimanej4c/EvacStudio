# Lower bounds exclude the versions flagged by pip-audit during the security
# audit; they are floors, not pins, so patch releases still install. A full
# freeze (pip freeze > requirements.lock) is recommended before deployment so a
# server can be rebuilt identically.
django>=5.2.17,<5.3; python_version < "3.12"   # LTS sécurisée, compatible Python 3.10/3.11
django>=6.0.8,<6.1; python_version >= "3.12"  # Série validée localement sur Python 3.12+
djangorestframework
djangorestframework-simplejwt
django-cors-headers
Pillow>=12.3.0              # PYSEC-2026-2253..2257, 3451..3453
pypdf>=6.15.0               # PYSEC-2026-3009..3656
sqlparse>=0.6.0             # PYSEC-2026-3696..3699
numpy
opencv-python
python-dotenv
dj-database-url
mysqlclient>=2.2.1          # Pilote MySQL recommandé par Django
psycopg2-binary
gunicorn
PyMuPDF
xai-sdk
